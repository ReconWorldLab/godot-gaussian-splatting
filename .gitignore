# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
# Local export configuration and its output (the Android preset writes a ~110 MB
# APK into output/). Export settings are per-developer; nothing here belongs to
# the plugin, which ships through .gitattributes' addons-only export.
export_presets.cfg
output/

# OS junk
.DS_Store

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

*.import
*.uid

# Large sample assets are distributed via GitHub Releases, not the repository.
samples/assets/demo.ply
samples/assets/demo.compressed.ply
